{
  "name": "background-game",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Artem K",
  "license": "MIT",
  "scripts": {
    "start": "concurrently \"nodemon --exec babel-node app.js\" \"cd client && yarn run start\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "yarn install && cd client && yarn install && yarn run build"
  },
  "engines": {
    "node": "13.12.0"
  },
  "dependencies": {
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0",
    "@babel/node": "^7.8.7",
    "@babel/preset-env": "^7.9.5",
    "bcrypt": "^4.0.1",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^2.0.3",
    "pg-promise": "^10.5.2",
    "socket.io": "^2.3.0",
    "validator": "^13.0.0"
  }
}
